name: Test Packaging

on:
  workflow_dispatch: {}

permissions:
  contents: read
  packages: read

concurrency:
  group: test-packaging-${{ github.ref }}
  cancel-in-progress: true

jobs:
  run:
    uses: ./.github/workflows/_test_template.yml
    with:
      pull_tag: latest
      test_files: |
        tests/packaging
    secrets: inherit
