# Conversion Pipeline Tests

This folder contains tests for public conversion pipeline behaviour that is not
owned by one SAD element family.

Use this folder for options and line-level behaviour such as excluded elements,
offset markers, reference-particle setup, reverse charge, line reversal, and
other `convert_sad_to_xsuite` contracts.
