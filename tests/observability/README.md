# Observability Tests

This folder contains tests for user-visible output policy.

Use these tests to protect quiet mode, verbose mode, logging, and suppression
of noisy helper output. Behavioural converter correctness belongs in
`tests/conversion/`.
