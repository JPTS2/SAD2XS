# Test Fixtures

This folder contains committed input files used by tests.

Fixtures should be small, named for the behaviour they support, and shared only
when sharing improves clarity. Prefer inline temporary lattices in a test when
the input is short and specific to one case.
