# SAD Helper Tests

This folder contains tests for `sad2xs.sad_helpers`.

Use these tests for SAD command generation, temporary-file handling, output
parsing, and smoke behaviour around helper execution. Converter physics
comparisons that consume helper output belong in `tests/conversion/`.
